name = "diztech-website"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Lets Pages Functions send email via Cloudflare Email Routing.
# Requires Email Routing to be enabled on the diztech.co.zw zone, with
# CONTACT_DESTINATION_EMAIL (set below as a Pages environment variable)
# added as a verified destination address.
[[send_email]]
name = "SEND_EMAIL"
